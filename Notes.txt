Making dockerfiles

dockerfiles are used to build images. 
list of commands to build images

FROM "base image" (python3 for this project)

WORKING "where the working directory will be"

ENV "port to run on" || EVIRONMENT VARIABLE

COPY "copy in a file"

RUN "

COPY

CMD "default command to run when starting container"


how to make an image 

docker build "builds image from Dockerfile"


API key = at_XGGEvybXDmcdBvTnBiNbQyGQekz6i